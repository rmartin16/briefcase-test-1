name: Dependabot Change Note

on:
  push:
    branches:
      - 'dependabot/**'

jobs:
  changenote:
    uses: rmartin16/.github-beeware-test/.github/workflows/dependabot-changenote.yml@main
    secrets: inherit
